using System;
using UnityEngine;
using UnityEngine.UI;

/*
public class StopwatchUI : MonoBehaviour
{
    public Stopwatch stopwatch;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        TimeSpan timeSpan = TimeSpan.FromSeconds(stopwatch.time);

        //        GetComponent<Text>().text = $"Time to next race: {timeSpan:mm\\:ss\\.ff}";
        GetComponent<Text>().text = $"Time to next race: {timeSpan.Seconds} seconds";
    }
}
*/